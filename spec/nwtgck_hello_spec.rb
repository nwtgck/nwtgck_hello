require 'spec_helper'

describe NwtgckHello do
  it 'has a version number' do
    expect(NwtgckHello::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
